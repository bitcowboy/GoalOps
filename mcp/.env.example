# PocketBase HTTP endpoint (default: http://127.0.0.1:8090)
POCKETBASE_URL=http://127.0.0.1:8090

# Option A — superuser credentials; the server logs in at startup
POCKETBASE_ADMIN_EMAIL=
POCKETBASE_ADMIN_PASSWORD=

# Option B — pre-acquired auth token (skips login)
POCKETBASE_AUTH_TOKEN=
