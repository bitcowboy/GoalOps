# Production frontend env — picked up by `vite build`.
#
# Same-origin: the SPA is served by Caddy at https://goal.996007.fun and PB is
# reverse-proxied at /api/, so we point the SDK at the site root and let it
# append the /api/... paths itself.
VITE_POCKETBASE_URL=https://goal.996007.fun
